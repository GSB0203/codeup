#include<stdio.h>
int main()
{
    int n, k, i, count[100001], a=1;
    scanf("%d %d", &n, &k);
    
    for(i=1;i<=n;i++)
        if(n%i==0)
			count[a++]=i;
			
	printf("%d", count[k]);
    return 0;
}

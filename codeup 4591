#include <stdio.h>

int main() {
    int n[9]={}, i, max = 0, flag = 0;
    for(i = 1 ; i <= 9 ; i++) {
        scanf("%d", &n[i]);
        if(n[i] > max) {
            max = n[i];
            flag = i;
        }
    }

    printf("%d\n%d", max, flag);
    return 0;
}
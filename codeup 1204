#include<stdio.h>
int main()
{
    int a, i;
    scanf("%d", &a);
    i=a%10;
    if(a>=11&&a<=13) printf("%dth", a);
    else if(i==1) printf("%dst", a);
    else if(i==2) printf("%dnd", a);
    else if(i==3) printf("%drd", a);
    else printf("%dth", a);
}

#include<stdio.h>
int main()
{
    int n, i, j, k;
    scanf("%d", &n);
    for(i=1;i<=n;i++)
    {
        for(j=1;j<=2;j++)
        {
            printf("*");
        }
        printf("\n");
        for(k=0;k<i;k++)
        {
            printf(" ");
        }
    }
}

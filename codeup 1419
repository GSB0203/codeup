#include<stdio.h>
int main()
{
    char n[100];
    int s=0;
    scanf("%[^\n]", &n);
    
    for(int i=0;n[i]!='\0';i++)
    {
        if(n[i]=='l'&&n[i+1]=='o'&&n[i+2]=='v'&&n[i+3]=='e')
            s+=1;
    }
    
    printf("%d", s);
}

#include<stdio.h>
int main()
{
    char a, b, i;
    scanf("%c %c", &a, &b);
    if(a>b)
        for(i=b;i<=a;i++)
            printf("%c ", i);
    else if(a<b)
        for(i=a;i<=b;i++)
            printf("%c ", i);
    else if(a=b) printf("%c", a);
    
}

#include<stdio.h>
int main()
{
    int a, b[10], i, s=0;
    for(i=0;i<=9;i++)
    {
        scanf("%d", &a);
        b[i]=a;
    }
        
    for(i=0;i<=9;i++)
        if(b[i]%5==0)
        {
            printf("%d", b[i]);
            s=1;
            break;
        }
    if(s==0) printf("0");
    return 0;
        
        
    
}

#include<stdio.h>
#include<algorithm>
using namespace std;

int n, i;
int arr[100001];

int main() {
    scanf("%d", &n);
    
    for(i=0;i<n;i++)
        scanf("%d", &arr[i]);
    
    sort(arr, arr+n);
    
    for(i=0;i<n;i++)
        printf("%d\n", arr[i]);
        
        
    return 0;
	
	
}

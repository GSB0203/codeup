#include<stdio.h>
int main()
{
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    int s=b-c;
    if(a>s) printf("do not advertise");
    else if(a<s) printf("advertise");
    else printf("does not matter");
}

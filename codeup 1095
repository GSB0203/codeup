#include<stdio.h>
int main()
{
    int i, n, a[10000], min=999999;
    scanf("%d", &n);
    
    for(i=1;i<=n;i++)
    {
        scanf("%d", &a[i]);
        if(min>a[i]) min=a[i];
    }
    printf("%d", min);
    return 0;
}


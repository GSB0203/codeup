#include<stdio.h>
#include<algorithm>
using namespace std;


int s[7]={};
int main() {
	for(int i=0;i<7;i++)
	    scanf("%d", &s[i]);
	    
	sort(s, s+7, greater<int>());
	
	printf("%d\n%d", s[0], s[1]);
	return 0;
	
	
}

#include<stdio.h>
int main()
{
    int i, n, sum=0, a[10000];
    scanf("%d", &n);
    sum=n*(n-1)/2+n;
    
    for(i=1;i<=n;i++)
    {
        scanf("%d\n", &a[i]);
        sum-=a[i];
    }
    printf("%d", sum);
    return 0;
}
